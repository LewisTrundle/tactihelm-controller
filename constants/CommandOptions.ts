export enum Pattern {
  SINGLE,
  WALL,
  WAVE
};

export enum Scheme {
  CUSTOM,
  CONSTANT,
  VARYING
}

export enum Tactor {
  REAR,
  MID,
  FRONT
}

export enum Scenario {
  REAL,
  SCENE1,
  SCENE2,
  SCENE3,
  SCENE4,
  SCENE5,
  SCENE6
}

export enum ActivationType {
  MANUAL,
  SENSOR
}

